import json
from pathlib import Path

ROOT=Path(__file__).resolve().parent.parent

def main():
    d=json.loads((ROOT/'reports/FINAL_ANALYSIS.json').read_text());base=d['baseline'];final=d['versions']['frozen_s7_r2'];ef=d['final_effects']
    rows=['| 固定 Actor | 累计平方 jerk：原层 → 新层 | 最坏 jerk：原层 → 新层 | 平均旅时 s：原层 → 新层 | 能耗变化 |','| --- | ---: | ---: | ---: | ---: |']
    for a,label in [('A','原 TD3（λ=0）'),('B','已有舒适性正则（λ=0.1）')]:
        b,n=base[a],final[a]
        rows.append(f"| {label} | {b['mean']['Ij']:.3f} → {n['mean']['Ij']:.3f}（{ef[a]['changes']['Ij']*100:.2f}%） | {b['peak_jerk']:.3f} → {n['peak_jerk']:.3f} | {b['mean']['time_s']:.3f} → {n['mean']['time_s']:.3f} | {ef[a]['changes']['E_Wh']*100:.2f}% |")
    secondary=[]
    for a in ['A','B']:
        s=d['secondary_original_cut'][a];old=s['original']['Ij'];new=s['new']['mean']['Ij']
        secondary.append(f"- {a}，沿用旧到达截点：I_j={old:.3f} → {new:.3f}（{100*(new/old-1):.2f}%）。")
    text=r'''# 舒适性模块 v2：从软正则到曲线可行约束

本轮完成了原轨迹诊断、固定时间停车可行性分析、结构执行层实现及开发探针。最终候选在两个固定 Actor 各九个开发条件中均完成行程，无红灯、物理边界违规或紧急回退，最坏离散 jerk 为 2 m/s³。没有训练新网络，没有启动正式三种子实验，没有改动原八种子论文数据。

## 对“一个正则够不够”的回答

单个有限权重的正则只改变偏好，不自动给出可行性保证。重点是先规定哪些速度变化可以安全且平顺地执行，再让 TD3 决定其偏好的加减速动作；无需叠加许多互相牵制的惩罚项。

若速度为二次多项式 v(t)=c₂t²+c₁t+c₀，加速度为 2c₂t+c₁，jerk 为 2c₂。它适合恒 jerk 的局部过程。但单段二次速度若同时要求 a(0)=a(T)=0，则 c₂=c₁=0，无法完成非零速度变化。因此停车应拼接逐渐加深制动、逐渐释放制动两个阶段，必要时加入恒减速度阶段。

以 s=t/T、Δv=v_f−v_0 为记号，一种对称的分段二次速度为

$$
v(t)=v_0+\Delta v\begin{cases}2s^2,&0\le s\le 1/2,\\1-2(1-s)^2,&1/2<s\le1.\end{cases}
$$

它使两端加速度为零，速度和加速度在中间连续，jerk 分段恒定。峰值加速度为 2|Δv|/T，峰值 jerk 为 4|Δv|/T²。只有满足实际加速度界和 jerk 界时，这个两段模板才可执行；不能用缩短 T 的方式无条件满足赶点要求。

在固定 T、两端 a=0 的理想标量模型中，任意峰值 jerk 上界 J 都满足 |Δv|≤JT²/4；上述两段曲线达到等号。因此它对控制最坏 jerk 很合适。若目标是最小化累计平方 jerk，则三次速度连接在相应固定端点条件下更合适。曲线阶次并非越高越好。

| 同一停车：16 → 0 m/s，12 s，96 m，两端 a=0 | 峰值加速度 | 峰值 jerk | 累计平方 jerk |
| --- | ---: | ---: | ---: |
| 分段二次速度 | 2.667 | 0.444 | 2.370 |
| 三次速度 | 2.000 | 0.667 | 1.778 |
| 五次速度 | 2.500 | 0.642 | 2.540 |

上表是解析运动学示例，不是 RL 车辆实验。五次曲线还使两端 jerk 为零，但在这个固定时间示例中累计平方 jerk 更高。图见 `reports/velocity_profiles.png`。

## 原实验具体缺在哪里

1. **停车释放阶段可能已不可行。** 两组终点评估共 62 个 jerk 超限子步，均落在当时 jerk 区间与安全/非负速度边界没有交集的状态；其中 20 个由低速或零速边界触发。此时重新调节动作正则不能让已为空的可行集合变成非空，需要提前限制制动状态。

2. **终点记录比信号灯停车少了一段。** 原任务在 x≥3999 m、v≤0.3 m/s 时结束，可能尚有非零减速度；信号停车后的减速度归零却继续计入指标。本轮给两组参考轨迹均补至静止，用相同终止定义比较。原 v1 报告和数据没有改写。

3. **全局软正则带来了行驶变慢。** 旧 B 比 A 的平均停车时间只多约 4.44 s，总旅时却多 32.11 s。移动阶段也明显变慢，不能只归咎于红灯等待。

在原离散模型中，令 h=0.5、q=Jh，当前负加速度大小为 b。以最大允许 jerk 释放制动至少还会损失

$$W_h(b)=h\sum_{i\ge1}(b-iq)_+$$

的速度。保持非负速度的必要且充分条件（仅针对释放制动、未计停车位置）是 v≥W_h(b)。对下一步候选减速度 b_new，还需 v≥h∑_{i≥0}(b_new−iq)_+。执行层直接使用这一条件，避免车辆到零速时仍保持过大的负加速度。

## 固定时间诊断：不需要通过全程降速解决大多数停车尖峰

从补齐尾段后的两组记录中取全部 25 段停车事件，每段取停车完成前最多 12 s。在保持原起始状态、结束位置、结束时间，且要求末端 v=a=0 的条件下，用原离散积分器的线性动力学、加速度界和 jerk 界求解平滑轨迹。

24/25 找到可行轨迹。剩下 A 条件 7 的一次信号停车，在同样边界下经独立线性规划确认不可行。这是离线可行性诊断，不是预知未来的算法基线，也不是 24 条独立学习重复。它支持“大多数停车有同等耗时的平滑空间”，不支持“所有停车都能无代价平滑”。

## 新执行层与 TD3 的接口

固定 Actor 仍输出原归一化动作，保持原正负加速度映射。执行层在每个 0.5 s 子步根据当前位置、速度、上一实际加速度和已知道路/信号约束，构造允许的加速度区间；在允许集合内取离 TD3 指令最近的动作。

集合包含：jerk 增量界、执行器界、速度非负及制动释放余量、下游停止/限速目标、道路速度上界。绿灯结束附近允许“可在变红前通过”与“可在停车线前停下”两种分支；其允许集合可能是两个区间的并集，不能错误地一律称为单个凸区间。无可行备份时记录并使用原紧急策略，不能宣称对任意突发事件都保证舒适性。

该层是当前状态的函数，不保存一个未进入观测的曲线倒计时。既有能量计算、道路、信号周期和 Actor 网络不变。实现沿用原分段常加速度子步，其速度采样对应分段二次趋势；这不是连续时间精确曲线积分的新车辆模型。

## 固定 Actor 的开发闭环结果

下表两边均包括停车归零尾段，分别在同一个 Actor 的九个条件内比较执行层前后。单位：累计平方 jerk 为 m²/s⁵，jerk 为 m/s³。

TABLE

最终 18/18 完成；两组均无红灯、物理边界违规、局部超速（1e−6 m/s 容差）和紧急回退。两个 Actor 各自的开发门槛全部通过。A 的时间增量只有约 0.11 s，因此这次改善没有伴随先前 11.9% 的全程旅时增加。

为避免“补尾段才制造改善”的疑问，同步按原到达截点重算：

SECONDARY

两种截点下改善均存在，但不能混用分母。B 加新层仍保留其原本较慢的驾驶策略；新执行层没有重新训练或消除 B 的整体速度偏好。因此后续主候选应优先考虑 A 加结构执行层，将旧软正则作为对照，而非默认把两者强行叠加。

## 开发过程中没有隐藏的结果

- r0：保留的第一实现。在 A 条件 7 发生一次红灯违例，未通过。另有弯道速度目标与完全停车目标混用造成的错误空区间。
- r1：修正上述实现，18/18 完成，最坏 jerk 为 2，零回退。但 A 的最大超速幅度从 0.612 增至 0.744 m/s，未满足原比较门槛。
- r2：明确将道路 80 km/h 上界纳入同一可行集合。最终全部开发条件通过。

r2 是明确的方法范围修订，不是只改一个图形，也不能把全部增益归于“二次曲线”。它同时修正了轨迹可行性、绿灯结束可达性和道路速度约束。三个版本的全部 54 条闭环记录及早期源码快照均保留。全程没有搜索 λ、换种子或挑检查点，也没有新增训练步。

## 证据强度与论文安排

本轮使舒适性模块从“加惩罚后变慢”推进到“固定策略下，以接近原旅时执行满足曲线可行约束的动作”。这是值得保留的开发进展；独立训练 seed 仍只有一个，A/B 是同一 seed 的两个策略版本。未开展正式多种子检验、联合重新训练或实车/乘客评价。

原 v17 的理论主线可保持。此模块应定位为实际执行可行性的补强，不把理想价值理论的最优 bang-bang 动作直接认作舒适驾驶。后续若联合训练，Critic 的转移数据必须来自含该执行层的实际环境，不能只在部署时切换执行层却声称已完成端到端 TD3 验证。

动作正则已有 CAPS 等工作；受 jerk 约束的在线轨迹生成也是成熟方向。原创性应集中在本论文任务中学习目标、执行可行性和实际指标的一致结合，而非认领这些一般原理。[CAPS 原文](https://arxiv.org/abs/2012.06644)；[RSS 2021 轨迹生成论文](https://arxiv.org/abs/2105.04830)；[Ruckig 可行性说明](https://docs.ruckig.com/tutorial.html)。

## 核验与复现

`audit/layer_audit.json` 包括 646 项释放/动作检查、395 组停车距离单调性检查、150 组正速度目标检查，以及 60 条变灯边界轨迹。它们是实现核验，不代替对任意场景的证明。

`code/summarize.py` 不导入训练指标函数，直接从全部 54 条新闭环轨迹核算 jerk、时间、能耗、超速和完成状态；原始统计 270 项数值比对通过。`reports/FINAL_ANALYSIS.json` 保存全部比较与门槛，`protocol/IMPLEMENTATION_FIXES.md` 记录开发修正。

所有神经网络保持冻结。本轮计算仅有诊断、解析曲线、线性/二次优化及短闭环评估；10 分钟烟测和 1 小时小规模均作为上限，完成固定条件就停止。没有启动先前停止的四配置训练，也没有打开原正式报告集。
'''
    text=text.replace('TABLE','\n'.join(rows)).replace('SECONDARY','\n'.join(secondary))
    (ROOT/'reports/RESULTS_v2.md').write_text(text)
    print(ROOT/'reports/RESULTS_v2.md')

if __name__=='__main__':main()
