# Distribution status

Local source and wheel release: EvcRL 0.0.1. Not published to PyPI; the distribution name `evcrl` is provisional and its availability on the index has not been checked.

Licence: Apache License 2.0, taken from the research repository (`anonymous-rl-lab/evcRL`, file `LICENSE`) that contains the archived sources this package derives from. The manuscript's pretrained weights, logs and training framework are not bundled.

Verification of this release (clean virtual environments, wheel install, test suite, smoke runs, equivalence against the archived code) is recorded in `SOFTWARE_VERIFICATION.json`.
